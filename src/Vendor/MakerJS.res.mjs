// Generated by ReScript, PLEASE EDIT WITH CARE


function make(fillOpt, strokeOpt, strokeWidthOpt, fillRuleOpt, scalingStrokeOpt, accuracyOpt, annotateOpt, classNameOpt, cssStyleOpt, scaleOpt, viewboxOpt) {
  var fill = fillOpt !== undefined ? fillOpt : "none";
  var stroke = strokeOpt !== undefined ? strokeOpt : "black";
  var strokeWidth = strokeWidthOpt !== undefined ? strokeWidthOpt : 0.25;
  var fillRule = fillRuleOpt !== undefined ? fillRuleOpt : "evenodd";
  var scalingStroke = scalingStrokeOpt !== undefined ? scalingStrokeOpt : true;
  var accuracy = accuracyOpt !== undefined ? accuracyOpt : 0.001;
  var annotate = annotateOpt !== undefined ? annotateOpt : false;
  var className = classNameOpt !== undefined ? classNameOpt : "";
  var cssStyle = cssStyleOpt !== undefined ? cssStyleOpt : "";
  var scale = scaleOpt !== undefined ? scaleOpt : 1;
  var viewbox = viewboxOpt !== undefined ? viewboxOpt : true;
  return {
          fill: fill,
          stroke: stroke,
          strokeWidth: strokeWidth,
          fillRule: fillRule,
          scalingStroke: scalingStroke,
          accuracy: accuracy,
          annotate: annotate,
          className: className,
          cssStyle: cssStyle,
          scale: scale,
          viewbox: viewbox
        };
}

var Options = {
  make: make
};

var TextModel = {};

var Character = {};

export {
  Options ,
  TextModel ,
  Character ,
}
/* No side effect */
